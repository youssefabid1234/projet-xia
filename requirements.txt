# Application web, tuteur en terminal et modeles generes.
-r app/web/requirements.txt
Werkzeug>=3.1,<4

# Extraction PDF et indexation des cours (inclut PyMuPDF et tiktoken).
-r scripts/requirements-cours.txt

# Importe directement par les tests hors ligne.
httpx>=0.27,<1
